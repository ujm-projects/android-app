<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.faircorp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Faircorp"
        android:usesCleartextTraffic="true">
        <activity android:name=".CreateRoomActivity"
            android:parentActivityName=".MainActivity"></activity>
        <activity android:name=".HeatersActivity"
            android:parentActivityName=".RoomsActivity"/>
        <activity android:name=".RoomsActivity"
            android:parentActivityName=".BuildingList"/>
        <activity android:name=".BuildingList"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".WindowsActivity"
            android:parentActivityName=".RoomsActivity"/>
        <activity android:name=".BuildingWindowListActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".WindowActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>